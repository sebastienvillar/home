from dht import DHT22, read_retry
import sys

if __name__ == "__main__":
    # pin = int(sys.argv[1])
    # sensor = DHT22
    # retries = int(sys.argv[2])
    print('{ "hello": "hello" }')
    sys.stdout.flush()
    # read_retry(sensor, pin, retries)